import { WholeNumberDirective } from './whole-number.directive';

describe('WholeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new WholeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
