import { CombinedDatetimeMinDirective } from './combined-datetime-min.directive';

describe('CombinedDatetimeMinDirective', () => {
  it('should create an instance', () => {
    const directive = new CombinedDatetimeMinDirective();
    expect(directive).toBeTruthy();
  });
});
