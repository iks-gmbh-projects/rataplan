import { CheckboxCountMaxDirective } from './checkbox-count-max.directive';

describe('CheckboxCountMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxCountMaxDirective();
    expect(directive).toBeTruthy();
  });
});
