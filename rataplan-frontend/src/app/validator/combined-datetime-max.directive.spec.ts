import { CombinedDatetimeMaxDirective } from './combined-datetime-max.directive';

describe('CombinedDatetimeMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new CombinedDatetimeMaxDirective();
    expect(directive).toBeTruthy();
  });
});
