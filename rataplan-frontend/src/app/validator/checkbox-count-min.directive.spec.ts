import { CheckboxCountMinDirective } from './checkbox-count-min.directive';

describe('CheckboxCountMinDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxCountMinDirective();
    expect(directive).toBeTruthy();
  });
});
