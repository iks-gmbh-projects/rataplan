import { SomeNonWhitespaceDirective } from './some-non-whitespace.directive';

describe('SomeNonWhitespaceDirective', () => {
  it('should create an instance', () => {
    const directive = new SomeNonWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
